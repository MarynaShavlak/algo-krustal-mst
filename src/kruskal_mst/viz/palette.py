# -*- coding: utf-8 -*-
"""Єдина палітра кольорів і службові стилі для всіх схем."""

from __future__ import annotations

# --- граф ---
C_NODE = "#A8D8EA"        # заливка вершин
C_NODE_EDGE = "#2C6E8F"   # обведення вершин
C_BASE_EDGE = "#D9D9D9"   # звичайні (ще не використані) ребра
C_MST = "#2E8B57"         # ребра МОД (прийняті)
C_REJECT = "#E63946"      # відхилене ребро (цикл)
C_CONSIDER = "#F4A300"    # ребро, що саме розглядається

# Кольори для різних компонент зв'язності
COMP_PALETTE = [
    "#A8D8EA", "#F6C9C9", "#C9E4C5", "#FCE4A6",
    "#D7C9EC", "#F8C8DC", "#C9E9E9", "#E8D5B7",
]

# --- підсвічування рядків коду ---
HL_ACTIVE = "#FFF1A8"     # рядок виконується зараз
HL_ADD = "#C7E9C0"        # спрацювала гілка «додати ребро»
HL_SKIP = "#F7C5C5"       # спрацювала гілка «пропустити (цикл)»

# --- дерево DSU ---
ROOT_BORDER = "#C8860D"   # рамка кореня
A_DSU = "#9AA0A6"         # стрілки вказівників на батька
FOUND_BORDER = "#1E5E3A"  # рамка знайденого кореня (під час find)
A_HI = "#F08A00"          # підсвічена стрілка / ребро обходу

# --- стани вершин і ребер в анімаціях та покрокових панелях ---
S_QUEUE = "#FCE08A"       # вузол у черзі (BFS)
S_DONE = "#D5DBDE"        # відвіданий вузол (BFS)
C_VISIT = "#FCD9A6"       # відвіданий вузол (порівняння на кроці 8)
E_BASE = "#C7CCD0"        # ребро лісу, ще не пройдене (BFS)
E_FAINT = "#EDEFF1"       # ледь помітне ребро поза лісом (BFS)
C_FAINT_DOT = "#E3E6E8"   # ледь помітне ребро поза лісом (порівняння)
C_PANEL_EDGE = "#DADDE0"  # базове ребро у покрокових панелях (forest + has_path)

# Непрозора плашка під підпис ваги (щоб число читалось поверх ребра)
CHIP = dict(boxstyle="round,pad=0.18", fc="white", ec="#cfcfcf", lw=0.6)
